{
  "name": "@tinman/bridge",
  "version": "0.0.0",
  "description": "Monorepo for AI agents and tools",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "clean": "turbo run clean",
    "lint": "turbo run lint",
    "clean:all": "rimraf **/node_modules"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "^0.5.3",
    "@workspace/configs": "*",
    "prettier": "^3.5.1",
    "rimraf": "^6.0.1",
    "turbo": "^2.4.2",
    "typescript": "^5.7.3",
    "wrangler": "^3.109.2"
  },
  "packageManager": "npm@10.0.0"
}
